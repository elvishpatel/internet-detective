function renderTimeline(items) { const el = document.querySelector('#timeline'); el.innerHTML = items.length ? items.map((x,i)=>`<article class="time-item ${x.stance}"><span>${String(i+1).padStart(2,'0')}</span><div><small>${escapeHtml(x.date || 'Undated record')}</small><a href="${safeUrl(x.url)}" target="_blank" rel="noopener">${escapeHtml(x.title)}</a><p>${badge(x.stance)} public record added to this case.</p></div></article>`).join('') : '<p class="empty">No dated public records were extracted from the retrieved documents.</p>'; }
