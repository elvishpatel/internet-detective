function renderGraph(graph) { const host = document.querySelector('#graph'); const nodes = graph.nodes || []; const links = graph.links || []; const w=760,h=390,cx=w/2,cy=h/2; const positioned=nodes.map((n,i)=>({...n,x:i===0?cx:cx+Math.cos((i-1)*Math.PI*2/Math.max(1,nodes.length-1))*(i%2?230:150),y:i===0?cy:cy+Math.sin((i-1)*Math.PI*2/Math.max(1,nodes.length-1))*145})); const find=id=>positioned.find(n=>n.id===id); host.innerHTML=`<svg viewBox="0 0 ${w} ${h}" aria-label="Evidence graph"><defs><filter id="glow"><feGaussianBlur stdDeviation="2" result="b"/><feMerge><feMergeNode in="b"/><feMergeNode in="SourceGraphic"/></feMerge></filter></defs>${links.map(l=>{let a=find(l.source),b=find(l.target);return a&&b?`<line x1="${a.x}" y1="${a.y}" x2="${b.x}" y2="${b.y}" class="link ${l.type}"/>`:''}).join('')}${positioned.map(n=>`<g class="node ${n.type}" transform="translate(${n.x},${n.y})"><circle r="${n.type==='claim'?48:30}"/><text text-anchor="middle" dy="5">${escapeHtml(n.label).slice(0,18)}</text></g>`).join('')}</svg><p class="graph-key"><i class="support"></i> supporting link <i class="contradict"></i> contradicting link <i class="entity"></i> entity relationship</p>`; }
